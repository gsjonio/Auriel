# Example environment variables
# For local development with SQLite (persistent file):
DATABASE_URL=sqlite+aiosqlite:///./auriel.db
# For PostgreSQL (production/dev with Docker) use something like:
# DATABASE_URL=postgresql+asyncpg://user:pass@db:5432/auriel
